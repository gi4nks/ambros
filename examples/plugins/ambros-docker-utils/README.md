# Ambros Docker Utils Plugin

A plugin for managing Docker resources.

## Commands

- `cleanup`: Removes stopped containers, dangling images, and unused volumes and networks.
- `stats`: Shows a real-time view of container resource usage.

## Installation

Install from a local path:
`ambros plugin install ./examples/plugins/ambros-docker-utils`
